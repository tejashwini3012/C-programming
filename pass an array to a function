C Program to pass an array to a function
#include<stdio.h>
void printarray(int a[])
{
    printf("size of the array in the fun: %d\n",sizeof(a));
    printf("print array:\n");
    for(int i=0;i<5;i++)
    {
        printf("%d\n",a[i]);
    }
}
int main()
{
    int a[5]={10,20,30,40,50};
    printf("size of array in main():%d\n",sizeof(a));
    printarray(a);
    return 0;
}

output
size of array in main():20
size of the array in the fun: 8
print array:
10
20
30
40
50
